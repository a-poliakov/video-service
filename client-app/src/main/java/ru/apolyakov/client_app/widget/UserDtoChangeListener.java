package ru.apolyakov.client_app.widget;

import javafx.beans.value.ChangeListener;
import javafx.beans.value.ObservableValue;
import ru.apolyakov.client_app.model.UserDto;

public class UserDtoChangeListener implements ChangeListener<UserDto> {
    @Override
    public void changed(ObservableValue<? extends UserDto> observable, UserDto oldValue, UserDto newValue) {

    }
}
